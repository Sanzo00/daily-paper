[project]
name = "daily-paper"
version = "0.1.0"
description = "Collect and publish daily arXiv papers by topic."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "arxiv>=2.2.0",
    "pyyaml>=6.0.2",
    "requests>=2.32.0",
]

[tool.uv]
package = false
